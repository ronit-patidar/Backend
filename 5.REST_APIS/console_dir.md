# 📘 console.dir()

## 🔹 Definition
`console.dir()` is used to display an **object’s properties** in a structured (tree-like) format in the console(in brower devtools).

---

## 🔹 Syntax
```js
console.dir(object);